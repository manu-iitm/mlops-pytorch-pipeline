data:
  data_dir: "./data"
  num_workers: 4

training:
  epochs: 20
  learning_rate: 0.001
  early_stopping_patience: 4
  batch_size: 64
  device: "cuda"

model:
  architecture: "resnet18"
  num_classes: 10

output:
  checkpoint_dir: "./checkpoints"
  model_name: "model.pt"